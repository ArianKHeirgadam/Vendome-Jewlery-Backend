# تحویل Phase 7B — صفحات عملیاتی

این نسخه تمام آیتم‌های منوی دسکتاپ را به صفحه و مسیر واقعی متصل می‌کند. داده‌های
داشبورد و نام کاربر از API خوانده می‌شوند؛ نام کامل در منوی پروفایل و کلمهٔ اول
`DisplayName` در عبارت خوش‌آمدگویی نمایش داده می‌شود.

این بسته شامل Hotfix 1 نیز هست: فایل قدیمی `dashboard.mock.ts` که ممکن است پس
از Extract روی سورس Phase قبلی باقی بماند، با نسخهٔ سازگار بازنویسی می‌شود تا
خطای TypeScript مربوط به `QuickOperation.path` رخ ندهد.

Hotfix 2 نیز نحوهٔ قراردادن خروجی React در برنامهٔ دسکتاپ را اصلاح می‌کند.
فایل‌های دارای نام هش‌شدهٔ Vite دیگر هنگام بازشدن پروژه در MSBuild ثبت
نمی‌شوند؛ فهرست واقعی `dist` پس از پایان Build خوانده می‌شود. در نتیجه تغییر
نام فایل‌های `index-*.css` و `index-*.js` باعث خطای `Could not copy` نمی‌شود.

## اجرای نسخه روی ویندوز

1. اجرای API، Worker و برنامهٔ دسکتاپ قبلی را متوقف و Visual Studio را ببندید.
   این نسخه را در یک پوشهٔ تازه Extract و Solution را از همان پوشه باز کنید.
2. در PowerShell و از ریشهٔ سورس جدید، کنترل کامل را اجرا کنید:

   ```powershell
   .\scripts\verify-phase7b.ps1
   ```

3. migration افزایشی را روی همان دیتابیس برنامه اعمال کنید:

   ```powershell
   .\scripts\apply-phase7b-migration.ps1
   ```

   اسکریپت در Development ابتدا environment variable و سپس User Secrets پروژهٔ
   API را بررسی می‌کند. برای دیتابیس دیگری، connection string را صریح بدهید:

   ```powershell
   .\scripts\apply-phase7b-migration.ps1 -ConnectionString "<connection string>"
   ```

4. پروژه‌های `GoldInvoice.Api`، `GoldInvoice.Worker` و
   `VendomeJewleryDesktopApp` را دوباره اجرا کنید و یک‌بار از حساب خارج و وارد
   شوید تا permissionهای جدید داخل توکن تازه قرار بگیرند.

migrationهای قبلی تغییر نکرده‌اند. migration جدید فقط جدول‌های
`business.Suppliers` و `crm.CustomerInteractions` و ایندکس‌ها/محدودیت‌های مرتبط
را اضافه می‌کند.

## کنترل مورد انتظار

- تمام ۱۲ آیتم منو باز می‌شوند و صفحهٔ مستقل دارند.
- فرم‌های مشتری، نشانی، محصول/مدل/قیمت، انبار/رسید، سفارش/تسویه، مدیر،
  تأمین‌کننده، CRM و مشخصات فروشگاه به API متصل‌اند.
- اگر `DisplayName` حساب در دیتابیس «مدیر وندوم» باشد، بالای برنامه همان نام کامل
  و روی داشبورد «سلام، مدیر» دیده می‌شود.
- چاپگر، ثبت دستگاه و صف چاپ عمداً برای Phase 7C باقی مانده‌اند.

در محیط ساخت این بسته SDK دات‌نت موجود نبود؛ بنابراین کنترل TypeScript، بیلد
production و audit فرانت انجام شده، اما بیلد کامل Solution/WPF و تست‌های دات‌نت
باید با اسکریپت بالا روی ویندوز اجرا شوند.
